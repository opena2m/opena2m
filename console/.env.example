# Console environment (Vite)
# Copy to .env.local and edit

# Gateway URL (leave blank to use Vite proxy in dev, set in prod)
VITE_GATEWAY_URL=

# Bearer token for API calls (dev-token in development)
VITE_GATEWAY_TOKEN=dev-token
